<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Couleur de fond -->
    <solid android:color="@android:color/white" />

    <!-- Coins arrondis (ici 16dp) -->
    <corners android:radius="16dp" />

    <!-- Bordure -->
    <stroke
        android:width="1dp"
        android:color="#E0E0E0" />
</shape>
